/**
 * Fixtures 통합 export
 *
 * 모든 테스트 데이터를 한 곳에서 import할 수 있도록 합니다.
 *
 * 가이드라인:
 * - 하드코딩된 테스트 데이터 사용 금지
 * - E2E 테스트와 통합 테스트에서 공통으로 사용
 * - 외부 API 모킹은 E2E 테스트에서만 사용 (에러 시나리오에 한해)
 */

export * from './api';
